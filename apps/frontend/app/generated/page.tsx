import Link from "next/link";

const businessHighlights = [
  "Business artifacts generated by the BA stage will appear here after a crew run.",
  "Frontend delivery stays isolated under app/generated for controlled rollout.",
  "Backend and QC outputs can be surfaced here as the workflow evolves."
] as const;

export default function GeneratedPage() {
  return (
    <main className="min-h-screen bg-[radial-gradient(circle_at_top,_#fef3c7,_#f8fafc_42%,_#e0f2fe)] px-6 py-10 text-slate-950">
      <div className="mx-auto max-w-5xl rounded-[36px] bg-white p-8 shadow-soft">
        <p className="text-sm font-semibold uppercase tracking-[0.18em] text-slate-500">
          Generated delivery
        </p>
        <h1 className="mt-4 text-4xl font-semibold leading-tight">
          Frontend production output will be refreshed by the crew.
        </h1>
        <p className="mt-5 max-w-3xl text-base leading-7 text-slate-700">
          This initial page is a stable target for the frontend delivery stage. After the first run,
          the IT department workflow can replace this file with a request-specific production page.
        </p>
        <ul className="mt-8 grid gap-4 md:grid-cols-3">
          {businessHighlights.map((item) => (
            <li key={item} className="rounded-2xl bg-slate-50 p-5 text-sm leading-7 text-slate-700">
              {item}
            </li>
          ))}
        </ul>
        <div className="mt-8">
          <Link
            href="/"
            className="inline-flex rounded-full bg-slate-950 px-5 py-3 text-sm font-semibold text-white transition hover:bg-slate-800"
          >
            Return to command center
          </Link>
        </div>
      </div>
    </main>
  );
}
